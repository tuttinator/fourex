# Docs

Project documentation lives here, grouped by area:

- `agents/` - agent usage and MCP integration notes
- `backend/` - backend and persistence documentation
- `frontend/` - frontend setup notes
- `plans/` - PRDs and implementation plans

Top-level `README.md` stays at the repo root as the main entrypoint.
